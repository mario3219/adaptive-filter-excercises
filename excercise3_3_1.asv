clc,clear

p = [6 4]';
R = [2 1; 1 2];
w0 = [0 0]';

v = w(30)

function w_next = w(n)
    my = 0.5*1/3;
    R = [2 1; 1 2];
    p = [6 4]';

    if n <= 0
        w_next = [100 100]';
    else
        w_next = w(n-1)+my*(p-R*w(n-1))
    end
end